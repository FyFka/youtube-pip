{
  "name": "youtube-pip-button",
  "version": "1.0.0",
  "private": true,
  "description": "YouTube extension that adds a Picture-in-Picture button to the native player controls.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint .",
    "check": "npm run lint && npm run build",
    "clean": "node ./scripts/clean.mjs",
    "package": "npm run clean && npm run build && node ./scripts/package.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.36.0",
    "@types/node": "^22.15.33",
    "eslint": "^9.36.0",
    "globals": "^16.4.0",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.44.0",
    "vite": "^7.1.7"
  }
}
